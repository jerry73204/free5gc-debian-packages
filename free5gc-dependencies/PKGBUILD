# Maintainer: Your Name <your.email@example.com>

pkgname=free5gc-dependencies
pkgver=1.0.0
pkgrel=1
pkgdesc="Meta-package for Free5GC dependencies"
arch=('all')
url="https://free5gc.org/"
license=('Apache')
depends=(
    'git'
    'gcc'
    'g++'
    'cmake'
    'autoconf'
    'libtool'
    'pkg-config'
    'libmnl-dev'
    'libyaml-dev'
    'dkms'
    'libyaml-cpp-dev'
    'curl'
    'wget'
    'gnupg2'
    'software-properties-common'
    'apt-transport-https'
    'ca-certificates'
    'lsb-release'
    'dirmngr'
)
# These will be added by the postinst script
# 'mongodb-org'
# 'golang-1.21'
# 'nodejs'
# 'yarn'
source=()
sha256sums=()
postinst="postinst.sh"
